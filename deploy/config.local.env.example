# Copy to config.local.env and fill in secrets.
# config.local.env is gitignored — never commit tokens or keys.

# Cloudflare Named Tunnel token (from Cloudflare Zero Trust dashboard)
# CLOUDFLARE_TUNNEL_TOKEN=eyJh...
